label chapter_01:
    # Order the scenes for this release.
    call screen character_select    # call selection screen (will Jump to scene_1_start on selection)
    # If you prefer selection only once, replace with calls to scenes directly:
    # call scene_1_start
    # call scene_2_start
    return